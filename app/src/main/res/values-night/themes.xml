<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Night-mode theme overrides.
        The color resources (background_dark, card_dark, text_primary, etc.)
        are overridden in values-night/colors.xml, so no extra style overrides
        are needed here. The DayNight theme engine picks them up automatically.
    -->
    
    <style name="Widget.PlayStation.FAB" parent="Widget.Material3.FloatingActionButton.Primary">
        <item name="elevation">4dp</item>
    </style>

    <style name="Widget.PlayStation.FAB.Extended" parent="Widget.Material3.ExtendedFloatingActionButton.Primary">
        <item name="elevation">4dp</item>
    </style>
</resources>